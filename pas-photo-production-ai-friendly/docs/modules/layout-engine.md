# layout-engine.js

**Role:** layout/nesting domain.

It is intentionally UI-independent. The public boundary is `PFLayout.generate(job)`.

The engine includes reference templates for standard passport photo mixes on A4/A5 and a fallback horizontal-shelf packing strategy for other cases.
